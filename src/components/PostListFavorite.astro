---
const { posts } = Astro.props;
---
{posts.map(post => (
  <div class="col-6"><a href={post.url} class="image fit" rel='nofollow'><img src={post.image} alt={post.alt} width = '100%' height = '100%' border = '0'/></a></div>
))}
