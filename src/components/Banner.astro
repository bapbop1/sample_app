---
const { post } = Astro.props;
---

<section class="bg-gray-100 p-6 rounded-lg mb-6">
  <h2 class="text-3xl font-bold mb-2">🔥 Bài viết nổi bật</h2>
  <img src={post.image} alt={post.title} class="rounded-lg w-full mb-4 max-h-80 object-cover" />
  <h3 class="text-2xl">{post.title}</h3>
  <p class="text-sm text-gray-600 mt-1">⭐ {post.rating} / 5</p>
  <p class="mt-2">{post.body.slice(0, 150)}...</p>
</section>
