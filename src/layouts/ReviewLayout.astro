---
const { title, rating, image, pros, cons } = Astro.props;
---

<article style="padding: 1rem; border: 1px solid #ccc; border-radius: 8px; margin-bottom: 2rem;">
  <h2>{title}</h2>
  <img src={image} alt={title} width="100%" />
  <p>⭐ {rating} / 5</p>

  <h3>Ưu điểm</h3>
  <ul>
    {pros.map(p => <li>✅ {p}</li>)}
  </ul>

  <h3>Nhược điểm</h3>
  <ul>
    {cons.map(c => <li>❌ {c}</li>)}
  </ul>

  <slot />
</article>
